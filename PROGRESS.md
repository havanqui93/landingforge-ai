# Project Progress Log

## Status
Last updated: 2026-07-18
Current focus: Correctness of the daily-automation pipeline + establishing a test safety net

## Completed
- [x] fix(news): match sensitive-keyword blocklist on word boundaries instead of substrings — 2026-07-17
- [x] feat(ai): upgrade keyword→LandingConfig generator from claude-sonnet-4-6 to claude-sonnet-5 — 2026-07-17
- [x] test: add vitest suite (slug, isSensitive, generateLanding) + `npm test` / `test:watch` — 2026-07-17
- [x] feat(ai): validate AI-generated LandingConfig at runtime with zod; fallback to generateLanding() on bad output — 2026-07-18
- [x] ci: add GitHub Actions workflow running typecheck + lint + test + build on PRs — 2026-07-18
- [x] refactor: consolidate two slugify implementations — generate-landing now reuses lib/slug.ts — 2026-07-18

## In Progress
- [ ] <none — session ended at a clean stopping point>

## Backlog (prioritized)
1. Make `fetchTopStory` selection logic testable (extract pure filter/sort or inject `fetch`).
2. Test `uniqueSlug` with a prisma mock.
3. Add blocklist variants ("killings", "deadly", …) if they surface in generated pages now that matching is stricter.

## Known Issues / Tech Debt
- Daily automation needs Vercel KV env vars (`KV_REST_API_URL`, `KV_REST_API_TOKEN`) in production and `ANTHROPIC_API_KEY` for the AI generator; both degrade gracefully when absent (local store / deterministic fallback), so the pipeline can't be tested end-to-end without those credentials.
- CI build step (`npm run build`) pre-renders static landings and requires no credentials, but can't exercise the KV or AI paths.

## Session Notes
Resumed after context compaction; baseline was green (20 tests passing, typecheck/lint clean). Picked 3 items from the top of the backlog:

- **Zod validation of AI config** — the highest-impact safety item. Replaced the silent `as LandingConfig` cast in `ai-generate-landing.ts` with `parseLandingConfig()` from a new `lib/landing-schema.ts`. The existing try/catch already promotes errors to a deterministic fallback, so invalid Claude output now triggers that path cleanly instead of shipping a malformed page. 12 new tests (7 rejection cases + valid paths + all section types).
- **CI workflow** — `.github/workflows/ci.yml` triggers on push to main and `claude/**` and on PRs to main. Runs typecheck → lint → test → build. Prisma is generated by `postinstall` during `npm ci`; KV/Anthropic keys are absent in CI and both degrade gracefully.
- **Consolidate slugify** — removed the standalone `slugify` from `lib/generate-landing.ts` and imported the canonical one from `lib/slug.ts`. Applied `.slice(0, 40)` at the single call site. Removed the duplicated test block from `generate-landing.test.ts`; `slug.test.ts` already covers the shared implementation.

Result: 30 tests passing (up from 20), typecheck and lint clean. Three commits pushed to `claude/autonomous-dev-session-bzjvzi`. Remaining backlog is 3 items.
